# Optional — required only for Google Drive backup.
# Create at https://console.cloud.google.com/apis/credentials → OAuth 2.0 Client ID (Web).
# Add your domain (e.g. https://your-app.vercel.app) to "Authorized JavaScript origins".
# Without this, Backup / Restore options simply do nothing.
VITE_GOOGLE_CLIENT_ID=
